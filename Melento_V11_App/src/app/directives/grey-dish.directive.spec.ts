import { GreyDishDirective } from './grey-dish.directive';

describe('GreyDishDirective', () => {
  it('should create an instance', () => {
    const directive = new GreyDishDirective();
    expect(directive).toBeTruthy();
  });
});
